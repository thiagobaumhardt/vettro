services:
  - type: web
    name: vetdom
    runtime: docker
    dockerfilePath: ./Dockerfile.prod
    dockerContext: .
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vetdom-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRA_HORAS
        value: "8"
      - key: CORS_ORIGINS
        value: "*"
      - key: ADMIN_NOME
        value: Administrador
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_SENHA
        sync: false

databases:
  - name: vetdom-db
    plan: starter
    databaseName: vetdom
    user: vetdom
