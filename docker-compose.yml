services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: vettro
      POSTGRES_PASSWORD: vettro
      POSTGRES_DB: vettro
    ports:
      - "5432:5432"
    volumes:
      - vettro_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vettro"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      DATABASE_URL: postgresql+psycopg://vettro:vettro@db:5432/vettro
      CORS_ORIGINS: "*"
      JWT_SECRET: dev-secret-troque-em-producao
      JWT_EXPIRA_HORAS: "8"
      ADMIN_NOME: Administrador
      ADMIN_EMAIL: admin@vettro.local
      ADMIN_SENHA: vettro123
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
      - ./backend/alembic:/app/alembic

  frontend:
    image: node:20-alpine
    restart: unless-stopped
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    environment:
      VITE_BACKEND_URL: http://backend:8000
    depends_on:
      - backend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - vettro_frontend_node_modules:/app/node_modules

volumes:
  vettro_pgdata:
  vettro_frontend_node_modules:
